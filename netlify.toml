[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# Redirect all routes to index.html for SPA routing (excludes /.netlify/* automatically)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
